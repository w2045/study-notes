# 线性代数 · 第七章 · 理论题

> 用 Markdown + LaTeX 数学公式作答。每题在 `### 解答` 下方书写推导过程。

---

## Q1 ⭐ 判断可正交对角化

下列矩阵中哪些可正交对角化（$A = Q\Lambda Q^T$）？说明理由。

(a) $A = \begin{bmatrix} 1 & 2 \\ 2 & 1 \end{bmatrix}$

(b) $B = \begin{bmatrix} 1 & 1 \\ 0 & 1 \end{bmatrix}$

(c) $C = \begin{bmatrix} 3 & 0 \\ 0 & 3 \end{bmatrix}$

### 解答

（在此作答）

---

## Q2 ⭐ Rayleigh 商计算

对 $A = \begin{bmatrix} 4 & 0 \\ 0 & 1 \end{bmatrix}$ 和 $\mathbf{x} = [2, 1]^T$，计算 Rayleigh 商 $R(A, \mathbf{x})$。验证结果在 $\lambda_{\min}$ 与 $\lambda_{\max}$ 之间。

### 解答

（在此作答）

---

## Q3 ⭐ 谱分解基础

对 $A = \begin{bmatrix} 3 & 0 \\ 0 & 2 \end{bmatrix}$，写出其谱分解 $A = \lambda_1 P_1 + \lambda_2 P_2$，并验证 $P_1 + P_2 = I$ 和 $P_1 P_2 = 0$。

### 解答

（在此作答）

---

## Q4 ⭐⭐ Schur 分解实例

验证 $U = \begin{bmatrix} 1 & 0 \\ 0 & 1 \end{bmatrix}$ 和 $T = \begin{bmatrix} 1 & 2 \\ 0 & 3 \end{bmatrix}$ 构成 $A = \begin{bmatrix} 1 & 2 \\ 0 & 3 \end{bmatrix}$ 的 Schur 分解。$U$ 是否是酉矩阵？

### 解答

（在此作答）

---

## Q5 ⭐⭐ 谱定理应用：求 $A^5$

用 $A = Q\Lambda Q^T$ 计算 $A = \begin{bmatrix} 2 & 1 \\ 1 & 2 \end{bmatrix}$ 的 5 次幂 $A^5$。

### 解答

（在此作答）

---

## Q6 ⭐⭐ Jordan 块与 Jordan 标准形

判断以下矩阵的 Jordan 标准形是什么（直接观察，不需求 $P$）：

(a) $A = \begin{bmatrix} 2 & 1 \\ 0 & 2 \end{bmatrix}$

(b) $B = \begin{bmatrix} 2 & 0 \\ 0 & 2 \end{bmatrix}$

(c) $C = \begin{bmatrix} 2 & 1 & 0 \\ 0 & 2 & 0 \\ 0 & 0 & 3 \end{bmatrix}$

### 解答

（在此作答）

---

## Q7 ⭐⭐ Jordan 链

对 $A = \begin{bmatrix} 1 & 1 & 0 \\ 0 & 1 & 1 \\ 0 & 0 & 1 \end{bmatrix}$，找出特征向量和广义特征向量（Jordan 链）。写出 $P$ 和 $J$ 使 $A = PJP^{-1}$。

### 解答

（在此作答）

---

## Q8 ⭐⭐ Rayleigh 商的极值性质

证明 Rayleigh 商的最小值和最大值分别等于 Hermitian 矩阵的最小和最大特征值。

### 解答

（在此作答）

---

## Q9 ⭐⭐⭐ 谱分解与正交投影

对 $A = \begin{bmatrix} 3 & 1 \\ 1 & 3 \end{bmatrix}$，写出谱分解 $A = \lambda_1 P_1 + \lambda_2 P_2$。描述 $P_1$ 和 $P_2$ 作为正交投影算子的几何含义。

### 解答

（在此作答）

---

## Q10 ⭐⭐⭐ 判断可对角化的多种条件

对 $A = \begin{bmatrix} 0 & 1 \\ 0 & 0 \end{bmatrix}$：

(a) 求出其特征值和特征向量。
(b) 代数重数和几何重数分别是多少？
(c) $A$ 是否可对角化？若不能，给出其 Jordan 标准形。

### 解答

（在此作答）

---

## Q11 ⭐⭐⭐ 用 Jordan 型计算矩阵指数

对 $A = \begin{bmatrix} 1 & 1 \\ 0 & 1 \end{bmatrix}$，利用 Jordan 标准形计算 $e^{A}$。

### 解答

（在此作答）

---

## Q12 ⭐⭐⭐ Courant-Fischer 极小极大原理

用 Courant-Fischer 定理解释为什么一个 Hermitian 矩阵 $A$ 的第二大特征值 $\lambda_2$ 等于「在所有与 $\mathbf{v}_1$（最大特征向量）正交的单位向量上，Rayleigh 商的最大值」。并通过一个简单的 $2\times 2$ 对称矩阵例子验证。

### 解答

（在此作答）
