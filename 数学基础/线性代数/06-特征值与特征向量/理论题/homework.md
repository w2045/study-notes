# 线性代数 · 第六章 · 理论题

> 用 Markdown + LaTeX 数学公式作答。每题在 `### 解答` 下方书写推导过程。

---

## Q1 ⭐ 求 2x2 特征值与特征向量

求 $A = \begin{bmatrix} 3 & 0 \\ 0 & 5 \end{bmatrix}$ 的特征值和特征向量。

### 解答

（在此作答）

---

## Q2 ⭐ 求 2x2 特征多项式

写出 $A = \begin{bmatrix} 4 & 1 \\ 2 & 3 \end{bmatrix}$ 的特征多项式 $p_A(\lambda)$，并求特征值。

### 解答

（在此作答）

---

## Q3 ⭐ 验证特征值/向量关系

验证 $\mathbf{v} = [2, 1]^T$ 是否是 $A = \begin{bmatrix} 4 & 2 \\ 1 & 3 \end{bmatrix}$ 的特征向量。若是，对应的特征值是多少？

### 解答

（在此作答）

---

## Q4 ⭐⭐ 迹和行列式与特征值关系

已知 $3\times 3$ 矩阵 $A$ 的特征值为 $\lambda_1 = 1, \lambda_2 = 2, \lambda_3 = 3$。求 $\operatorname{tr}(A)$ 和 $\det(A)$。

### 解答

（在此作答）

---

## Q5 ⭐⭐ 3x3 特征值（三角矩阵）

求 $A = \begin{bmatrix} 2 & 5 & -3 \\ 0 & -1 & 4 \\ 0 & 0 & 6 \end{bmatrix}$ 的全部特征值。

### 解答

（在此作答）

---

## Q6 ⭐⭐ 对角化

将 $A = \begin{bmatrix} 2 & 1 \\ 1 & 2 \end{bmatrix}$ 对角化：写出 $P$, $\Lambda$, $P^{-1}$，并验证 $A = P\Lambda P^{-1}$。

### 解答

（在此作答）

---

## Q7 ⭐⭐ 判断可对角化

判断 $A = \begin{bmatrix} 1 & 0 \\ 0 & 1 \end{bmatrix}$ 和 $B = \begin{bmatrix} 1 & 1 \\ 0 & 1 \end{bmatrix}$ 是否可对角化，并说明理由。

### 解答

（在此作答）

---

## Q8 ⭐⭐ 特征空间维数

对 $A = \begin{bmatrix} 2 & 0 & 0 \\ 0 & 2 & 0 \\ 0 & 0 & 3 \end{bmatrix}$，求每个特征值的代数重数和几何重数。

### 解答

（在此作答）

---

## Q9 ⭐⭐⭐ 幂迭代法原理

解释幂迭代法为什么收敛到绝对值最大的特征值对应的特征向量。用 $A = P\Lambda P^{-1}$ 和 $\mathbf{v}_0$ 在特征向量基下的展开进行推导。

### 解答

（在此作答）

---

## Q10 ⭐⭐⭐ 对角化的应用：矩阵幂

利用对角化计算 $\begin{bmatrix} 1 & 2 \\ 0 & -1 \end{bmatrix}^{10}$。

### 解答

（在此作答）

---

## Q11 ⭐⭐⭐ 相似矩阵的特征值

若 $B = P^{-1}AP$，证明 $A$ 和 $B$ 有相同的特征多项式（因而有相同的特征值）。

### 解答

（在此作答）

---

## Q12 ⭐⭐⭐ 特征值的几何应用

设 $A = \begin{bmatrix} 2 & 1 \\ 1 & 2 \end{bmatrix}$ 是 $\mathbb{R}^2$ 上的线性变换。描述 $A$ 在特征向量方向上分别做了什么（从几何角度解释），并画出单位圆 $\|\mathbf{x}\| = 1$ 在 $A$ 下变成了什么形状。

### 解答

（在此作答）
