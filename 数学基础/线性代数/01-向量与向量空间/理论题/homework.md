# 线性代数 · 第一章 · 理论题

> 用 Markdown + LaTeX 数学公式作答。每题在 `### 解答` 下方书写推导过程。

---

## Q1 ⭐ 线性组合表示

将 $\mathbf{w} = [7, 11]^T$ 表示为 $\mathbf{v}_1 = [1, 2]^T$ 和 $\mathbf{v}_2 = [3, 1]^T$ 的线性组合。即求 $c_1, c_2$ 使得 $\mathbf{w} = c_1\mathbf{v}_1 + c_2\mathbf{v}_2$。

### 解答

（在此作答）

---

## Q2 ⭐ 线性相关/无关判定

判断以下向量组是线性无关还是线性相关。若相关，给出一个非零组合。

(a) $\mathbf{v}_1 = [1, 2]^T$, $\mathbf{v}_2 = [2, 4]^T$

(b) $\mathbf{v}_1 = [1, 0, 1]^T$, $\mathbf{v}_2 = [0, 1, 0]^T$, $\mathbf{v}_3 = [1, 1, 1]^T$

### 解答

（在此作答）

---

## Q3 ⭐ 判断子空间

下列集合是否是 $\mathbb{R}^2$ 的子空间？说明理由。

(a) $W_1 = \{[x, y]^T \in \mathbb{R}^2 \mid y = 2x\}$

(b) $W_2 = \{[x, y]^T \in \mathbb{R}^2 \mid y = 2x + 1\}$

(c) $W_3 = \{[x, y]^T \in \mathbb{R}^2 \mid x \geq 0\}$

### 解答

（在此作答）

---

## Q4 ⭐ Span 的几何含义

描述以下向量组的 span 是 $\mathbb{R}^3$ 中的什么几何对象（原点？直线？平面？还是整个 $\mathbb{R}^3$？）。

(a) $\mathbf{v}_1 = [1, 2, 3]^T$

(b) $\mathbf{v}_1 = [1, 0, 0]^T$, $\mathbf{v}_2 = [0, 1, 0]^T$

(c) $\mathbf{v}_1 = [1, 0, 1]^T$, $\mathbf{v}_2 = [2, 0, 2]^T$

(d) $\mathbf{v}_1 = [1, 0, 0]^T$, $\mathbf{v}_2 = [0, 1, 0]^T$, $\mathbf{v}_3 = [1, 1, 0]^T$

### 解答

（在此作答）

---

## Q5 ⭐⭐ 求基与维数

求子空间 $W = \{[x, y, z]^T \in \mathbb{R}^3 \mid x + y + z = 0\}$ 的一组基，并写出 $\dim W$。

### 解答

（在此作答）

---

## Q6 ⭐⭐ 验证向量空间公理

验证：$V = \{[x, y]^T \in \mathbb{R}^2 \mid x = 0\}$（即 $y$ 轴）在 $\mathbb{R}^2$ 的运算下是向量空间。只需验证加法封闭、标量乘法封闭、零元存在三条。

### 解答

（在此作答）

---

## Q7 ⭐⭐ 线性无关的一般性质

(a) 若 $\{\mathbf{u}, \mathbf{v}\}$ 线性无关，证明 $\{\mathbf{u} + \mathbf{v}, \mathbf{u} - \mathbf{v}\}$ 也线性无关。

(b) 若 $\{\mathbf{u}, \mathbf{v}, \mathbf{w}\}$ 线性无关，$\{\mathbf{u}, \mathbf{v}\}$ 是否必然线性无关？反过来呢？

### 解答

（在此作答）

---

## Q8 ⭐⭐ 维数公式应用

在 $\mathbb{R}^3$ 中，$U = \operatorname{span}\{[1, 0, 0]^T, [0, 1, 0]^T\}$（$xy$ 平面），$W = \operatorname{span}\{[0, 1, 0]^T, [0, 0, 1]^T\}$（$yz$ 平面）。

(a) 求 $\dim U$, $\dim W$, $\dim(U \cap W)$。

(b) 用维数公式求 $\dim(U + W)$，并验证 $U + W = \mathbb{R}^3$。

### 解答

（在此作答）

---

## Q9 ⭐⭐ 基的唯一表示

证明：若 $\mathcal{B} = \{\mathbf{b}_1, \ldots, \mathbf{b}_n\}$ 是向量空间 $V$ 的一组基，则 $V$ 中每个向量可以**唯一地**表示为 $\mathcal{B}$ 中向量的线性组合。

### 解答

（在此作答）

---

## Q10 ⭐⭐⭐ 子空间的交

$U = \{[x, y, z]^T \in \mathbb{R}^3 \mid x - y = 0\}$，$W = \{[x, y, z]^T \in \mathbb{R}^3 \mid y - z = 0\}$。

(a) 验证 $U$ 和 $W$ 都是 $\mathbb{R}^3$ 的子空间。

(b) 求 $U \cap W$ 的一组基和维数。

### 解答

（在此作答）

---

## Q11 ⭐⭐⭐ 线性无关与 Span 的关系

设 $\mathbf{v}_1, \ldots, \mathbf{v}_k$ 是 $\mathbb{R}^n$ 中的向量。证明：

若 $\mathbf{v}_1, \ldots, \mathbf{v}_k$ 线性相关，则其中至少有一个向量可以表示为其余向量的线性组合。

### 解答

（在此作答）

---

## Q12 ⭐⭐⭐ 多项式向量空间

记 $P_2$ 为所有次数 $\leq 2$ 的实系数多项式构成的向量空间。

(a) 写出 $P_2$ 的标准基和维数。

(b) 判断 $\{1 + x, 1 - x, x^2\}$ 是否是 $P_2$ 的一组基。

(c) 将 $p(x) = 3 + 2x + 5x^2$ 用 (b) 中的基表示。

### 解答

（在此作答）
