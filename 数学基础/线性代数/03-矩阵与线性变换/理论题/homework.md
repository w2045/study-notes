# 线性代数 · 第三章 · 理论题

> 用 Markdown + LaTeX 数学公式作答。每题在 `### 解答` 下方书写推导过程。

---

## Q1 ⭐ 矩阵-向量乘法

计算 $A\mathbf{x}$，其中

$$A = \begin{bmatrix} 2 & -1 & 0 \\ 0 & 3 & 1 \end{bmatrix}, \quad \mathbf{x} = \begin{bmatrix} 1 \\ 2 \\ -1 \end{bmatrix}$$

### 解答

（在此作答）

---

## Q2 ⭐ 矩阵乘法

计算 $AB$ 和 $BA$（若存在），其中

$$A = \begin{bmatrix} 1 & 2 \\ 3 & 4 \end{bmatrix}, \quad B = \begin{bmatrix} 0 & 1 \\ 1 & 0 \end{bmatrix}$$

### 解答

（在此作答）

---

## Q3 ⭐ 转置性质验证

对 $A = \begin{bmatrix} 1 & 2 \\ 3 & 4 \end{bmatrix}$，计算 $A^T$，并验证 $(A^T)^T = A$。

### 解答

（在此作答）

---

## Q4 ⭐ 对称与反对称分解

将 $A = \begin{bmatrix} 1 & 3 \\ -1 & 2 \end{bmatrix}$ 分解为对称部分与反对称部分之和。

### 解答

（在此作答）

---

## Q5 ⭐⭐ 求线性变换的矩阵

设 $T: \mathbb{R}^2 \to \mathbb{R}^3$ 定义为 $T([x, y]^T) = [x + y, 2x - y, x + 3y]^T$。求 $T$ 的矩阵表示，并计算 $T([2, -1]^T)$。

### 解答

（在此作答）

---

## Q6 ⭐⭐ 矩阵乘法结合律验证

对 $A = \begin{bmatrix} 1 & 0 \\ 2 & 1 \end{bmatrix}$, $B = \begin{bmatrix} 1 & 2 \\ 0 & 1 \end{bmatrix}$, $C = \begin{bmatrix} 3 \\ 1 \end{bmatrix}$，验证 $(AB)C = A(BC)$。

### 解答

（在此作答）

---

## Q7 ⭐⭐ $(AB)^T = B^T A^T$ 验证

对 $A = \begin{bmatrix} 1 & 2 \\ 3 & 4 \\ 5 & 6 \end{bmatrix}$, $B = \begin{bmatrix} 0 & 1 \\ -1 & 0 \end{bmatrix}$，计算 $(AB)^T$ 和 $B^T A^T$，验证相等。

### 解答

（在此作答）

---

## Q8 ⭐⭐ 迹的循环性质

设 $A = \begin{bmatrix} 1 & 2 \\ 3 & 4 \end{bmatrix}$, $B = \begin{bmatrix} 0 & -1 \\ 1 & 0 \end{bmatrix}$。计算 $\operatorname{tr}(AB)$ 和 $\operatorname{tr}(BA)$，验证相等。

### 解答

（在此作答）

---

## Q9 ⭐⭐ 正交矩阵判定

判断以下矩阵是否为正交矩阵：

(a) $Q_1 = \begin{bmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta \end{bmatrix}$

(b) $Q_2 = \begin{bmatrix} 1 & 1 \\ 1 & -1 \end{bmatrix}$

### 解答

（在此作答）

---

## Q10 ⭐⭐ 三角矩阵性质

设 $U = \begin{bmatrix} 1 & 2 & 3 \\ 0 & 4 & 5 \\ 0 & 0 & 6 \end{bmatrix}$, $V = \begin{bmatrix} 7 & 0 & 0 \\ 8 & 9 & 0 \\ 10 & 11 & 12 \end{bmatrix}$。计算 $UV$ 的对角线元素，验证 $UV$ 的对角线元素 = $U$ 和 $V$ 对应对角线元素之积。

### 解答

（在此作答）

---

## Q11 ⭐⭐⭐ Kronecker 积

对 $A = \begin{bmatrix} 1 & 0 \\ 0 & 1 \end{bmatrix}$ 和 $B = \begin{bmatrix} 1 & 2 \\ 3 & 4 \end{bmatrix}$，计算 $A \otimes B$ 和 $B \otimes A$，说明两者是否相等。

### 解答

（在此作答）

---

## Q12 ⭐⭐⭐ 线性变换的复合

设 $S: \mathbb{R}^2 \to \mathbb{R}^2$ 是逆时针旋转 $90^\circ$，$T: \mathbb{R}^2 \to \mathbb{R}^2$ 是将 $x$ 坐标拉伸为原来 2 倍。写出 $S$ 和 $T$ 的矩阵，并计算 $S \circ T$ 和 $T \circ S$ 的矩阵。两者是否相等？几何上说明原因。

### 解答

（在此作答）
