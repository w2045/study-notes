# 线性代数 · 第九章 · 理论题

> 用 Markdown + LaTeX 数学公式作答。每题在 `### 解答` 下方书写推导过程。

---

## Q1 ⭐ SVD 的基本形式

写出 $A = \begin{bmatrix} 3 & 0 \\ 0 & 2 \end{bmatrix}$ 的 SVD（$A = U\Sigma V^T$）。提示：$A$ 已接近对角矩阵。

### 解答

（在此作答）

---

## Q2 ⭐ 奇异值与范数

若 $2\times 2$ 矩阵 $A$ 的奇异值为 $\sigma_1 = 5, \sigma_2 = 3$，求 $\|A\|_2$ 和 $\|A\|_F$。

### 解答

（在此作答）

---

## Q3 ⭐ 紧 SVD

$A = \begin{bmatrix} 1 & 2 \\ 2 & 4 \end{bmatrix}$（秩 = 1），写出其紧 SVD。

### 解答

（在此作答）

---

## Q4 ⭐⭐ $A^T A$ 与奇异值关系

对 $A = \begin{bmatrix} 1 & 0 \\ 0 & 2 \\ 0 & 0 \end{bmatrix}$，计算 $A^T A$ 及其特征值。由此给出 $A$ 的奇异值。

### 解答

（在此作答）

---

## Q5 ⭐⭐ SVD 中的正交矩阵验证

对 $A = \begin{bmatrix} 2 & 0 \\ 0 & 1 \end{bmatrix}$，验证 $\mathbf{v}_1 = [1, 0]^T$, $\mathbf{v}_2 = [0, 1]^T$, $\mathbf{u}_1 = [1, 0]^T$, $\mathbf{u}_2 = [0, 1]^T$ 是否为左右奇异向量。$\mathbf{u}_2$ 的方向是否唯一？为什么？

### 解答

（在此作答）

---

## Q6 ⭐⭐ 低秩近似的 Eckart-Young 定理

$A = \begin{bmatrix} 3 & 1 \\ 1 & 3 \end{bmatrix}$ 的奇异值为 $\sigma_1 = 4, \sigma_2 = 2$。写出秩-1 近似 $A_1$ 及其 Frobenius 误差 $\|A - A_1\|_F$。

### 解答

（在此作答）

---

## Q7 ⭐⭐⭐ 伪逆与最小二乘

用 SVD 求 $A = \begin{bmatrix} 1 & 0 \\ 0 & 1 \\ 0 & 0 \end{bmatrix}$ 的伪逆 $A^+$，并用伪逆求 $A\mathbf{x} = [2, 3, 4]^T$ 的最小二乘解。

### 解答

（在此作答）

---

## Q8 ⭐⭐⭐ 图像压缩原理

一副 $100 \times 100$ 的灰度图像矩阵 $M$ 的奇异值衰减为 $\sigma_1 = 1000$, $\sigma_2 = 500$, $\sigma_3 = 100$, $\sigma_{4,\ldots,100} < 10$。若取 $k = 3$ 的截断 SVD，计算压缩比和能量保留比例。

### 解答

（在此作答）

---

## Q9 ⭐⭐⭐ 奇异值与特征值的关系

证明：若 $A$ 是实对称矩阵，则 $A$ 的奇异值 = $|A$ 的特征值 $|$（绝对值）。

### 解答

（在此作答）

---

## Q10 ⭐⭐⭐ 条件数与奇异值

$A = \begin{bmatrix} 100 & 0 \\ 0 & 0.01 \end{bmatrix}$。求 $\kappa_2(A)$，并从奇异值角度解释为什么 $A$ 病态。

### 解答

（在此作答）

---

## Q11 ⭐⭐⭐ SVD 与四个基本子空间

对 $A \in \mathbb{R}^{m \times n}$ 的 SVD $A = U\Sigma V^T$，用 $U$ 和 $V$ 的各列表示 $A$ 的列空间、行空间、零空间和左零空间的基。

### 解答

（在此作答）

---

## Q12 ⭐⭐⭐ 利用 SVD 做矩阵补全（概念题）

在推荐系统中，评分矩阵有许多空缺。解释为什么截断 SVD 重构可以用来预测缺失评分（低秩假设），并描述基本步骤。

### 解答

（在此作答）
