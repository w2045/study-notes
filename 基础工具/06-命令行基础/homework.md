# 基础工具 · 第六章 · 命令行基础 — 作业

> 在终端中实际运行这些命令。

---

## Q1 ⭐ 导航

完成以下操作，写出每个命令的输出：
- 打印当前工作目录
- 进入 `Study` 目录
- 列出里面的一级文件和目录
- 返回 home 目录

---

## Q2 ⭐ 文件操作

在终端中：
- 在 `~/Desktop/Study/` 下创建一个 `test_dir` 目录
- 在 `test_dir` 中创建一个 `test.txt` 空文件
- 删除 `test_dir` 及其中文件

---

## Q3 ⭐ 查看文件

用 `head` 查看 `PROGRESS.md` 的前 3 行。写出命令。

---

## Q4 ⭐⭐ 管道与搜索

用管道组合 `ls` 和 `grep`，列出 `Study/` 下所有包含「基础」的目录名。写出命令。

---

## Q5 ⭐⭐ 搜索

用 `grep -r` 在整个 Study 项目中搜索包含字符串 `def normalize` 的文件。写出命令和结果。

---

## Q6 ⭐⭐ 查找

用 `find` 找出 Study 项目中所有 `.py` 文件。写出命令。

---

## Q7 ⭐ 快捷键

在终端中：
- 按 `Ctrl + A` 跳到行首
- 按 `Ctrl + E` 跳到行尾
- 按 `Ctrl + U` 清空当前行

这些不需要写答案——实操即可。
