# 基础工具 · 第五章 · VS Code 高效技巧 — 作业

> 每道题都在 VS Code 里实际操作一遍。

---

## Q1 ⭐ 命令面板

用命令面板做三件事，并写出你输入了什么关键词：
- 切换颜色主题
- 打开设置 JSON
- 放大字体

---

## Q2 ⭐ 快速打开文件

用 `Cmd/Ctrl + P` 打开项目中 `PROGRESS.md`。不要用侧边栏点击。写出你输入了什么来匹配这个文件。

---

## Q3 ⭐ 多光标实战

打开 `homework.py`，练习：
- 用 `Alt + Click` 在 3 个不同位置加光标，同时打字
- 用 `Cmd/Ctrl + D` 选中所有 `pass`，全部替换为 `# TODO`

---

## Q4 ⭐⭐ 全局搜索

用全局搜索（`Cmd/Ctrl + Shift + F`）在项目中搜索所有包含 `def normalize` 的文件。写出找到的文件路径。

---

## Q5 ⭐ 终端

打开集成终端（`` Ctrl + ` ``），运行：
```bash
pwd
ls
```

写出当前目录。

---

## Q6 ⭐ 分屏

用 `Cmd/Ctrl + \` 分屏，左边打开 `notes.md`，右边打开 `homework.py`。这一题不需要写答案——做到就算完成。
